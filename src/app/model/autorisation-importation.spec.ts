import { AutorisationImportation } from './autorisation-importation';

describe('AutorisationImportation', () => {
  it('should create an instance', () => {
    expect(new AutorisationImportation()).toBeTruthy();
  });
});
