import { RenouvellementAgrement } from './renouvellement-agrement';

describe('RenouvellementAgrement', () => {
  it('should create an instance', () => {
    expect(new RenouvellementAgrement()).toBeTruthy();
  });
});
